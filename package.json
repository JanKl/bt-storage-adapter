{
  "name": "storage-adapter",
  "version": "1.0.0",
  "description": "An adapter to various (cloud-based) object stores. One has to request a base folder under which they can store objects in virtual sub folders using an assigned folder secret. The sub folders cannot be created manually but will be created automatically as one puts files into it.",
  "main": "index.js",
  "scripts": {
	"prestart": "npm install",
	"start": "node index.js"
  },  
  "keywords": [
    "swagger"
  ],
  "license": "Apache-2.0",
  "private": true,
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "swagger-tools": "0.10.1"
  }
}
